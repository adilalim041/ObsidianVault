# Knowledge: Frontend — Index

> React, Vite, Tailwind, Fabric.js, general UI patterns. No project-specific stuff here.

## Subareas (folders to be created as content arrives)

- `react/` — hooks, state, performance, patterns
- `tailwind/` — responsive tricks, customization
- `fabric-js/` — canvas, editor patterns
- `vite/` — build, dev server, optimizations

## Nodes

> Empty for now. Filled as we learn.

## How to add a node

1. Pick the right subfolder (or create one if a new subarea is needed).
2. File name: `kebab-case-topic.md`.
3. Add it to this `_index.md` with a one-line description.
4. Inside the node: short opening paragraph (what + when to use), then sections.
