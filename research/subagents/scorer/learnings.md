# Scorer — Learnings

> Auto-updated after each run.

---

## 2026-04-09
- Speech-to-speech pipelines are highly valuable for personal assistant projects but often come with complex dependency management due to multiple ML libraries.

## 2026-04-09
- Shadcn/ui's copy-paste model eliminates dependency risks while providing enterprise-grade components, making it ideal for dashboard projects.

## 2026-04-09
- MCP servers are particularly valuable for Adil because they let him test and prototype API integrations through Claude Desktop before committing to code changes.

## 2026-04-09
- Content generation APIs score higher when they offer multiple modalities (text, image, video, audio) since they can serve multiple use cases across projects.

## 2026-04-09
- RSS parsing libraries are highly relevant for content automation projects but unknown licenses create significant adoption risk for commercial use.

## 2026-04-09
- Small dependency footprints (11 total deps) score higher for ease of adoption, especially when core functionality doesn't require heavy ML or system-level dependencies.

## 2026-04-09
- React component libraries with built-in data visualization (like Tremor + recharts) score highly for CRM projects since they eliminate the need to build custom charts and dashboards from scratch.

## 2026-04-09
- License safety scores are penalized when README doesn't explicitly state the license, even for well-maintained projects - always check the actual LICENSE file in the repo.

## 2026-04-09
- Speech-to-speech pipelines from established ML organizations provide comprehensive voice interaction capabilities but require careful license verification and dependency isolation due to complex ML toolchain requirements.

## 2026-04-09
- Job queue libraries with PostgreSQL backends score highly for Node.js projects because they eliminate the need for separate Redis/message broker infrastructure while providing enterprise-grade reliability features.

## 2026-04-09
- TypeScript codebases with private field encapsulation (#syntax) and EventEmitter patterns often contain valuable production-ready patterns for error handling and lifecycle management that transfer well across projects.

## 2026-04-09
- React component libraries with built-in data visualization (like Tremor + recharts) score highly for CRM projects since they eliminate the need to build custom charts and dashboards from scratch.

## 2026-04-09
- License safety scores are penalized when README doesn't explicitly state the license, even for well-maintained projects - always check the actual LICENSE file in the repo.

## 2026-04-09
- Multi-provider LLM gateways with automatic failover score extremely high for content generation projects that need reliability across different AI services, even when license uncertainty exists.

## 2026-04-09
- Provider abstraction patterns with configuration-driven registries (like providers.json) are valuable architectural patterns that can be adapted from Python to Node.js for multi-service reliability.

## 2026-04-09
- Job queue libraries that include production-ready monitoring dashboards using the same UI stack (React + Tailwind + shadcn/ui) score higher because they provide both infrastructure and reusable frontend patterns.

## 2026-04-09
- PostgreSQL-based job queues are particularly valuable for multi-service architectures because they eliminate the need for separate message broker infrastructure while providing ACID guarantees.

## 2026-04-09
- Research ML frameworks often have severe version compatibility issues and heavyweight dependencies that make them impractical for web service integration, even when functionally relevant.

## 2026-04-09
- Unknown license status is particularly risky for enterprise-grade frameworks from major organizations, as they may have complex licensing terms not visible in the README.

## 2026-04-09
- Repositories with 93%+ documentation files and zero visible source code entry points are likely meta-repositories or incomplete projects, regardless of promising feature claims.

## 2026-04-09
- Node.js version upper bounds in package.json can create adoption blockers when they exclude current LTS versions that users are running.

## 2026-04-09
- Component libraries with unknown license status in README require immediate LICENSE file verification before adoption, as they could have restrictive commercial terms that make them unsuitable for SaaS products like Omoikiri.AI.

## 2026-04-09
- MCP (Model Context Protocol) servers can provide valuable API client patterns and error handling even when the primary MCP functionality isn't directly applicable to web service architectures.

## 2026-04-09
- Unknown license status is particularly problematic for AI service integrations where commercial API usage costs make license compliance critical for business operations.

## 2026-04-09
- RSS parsing libraries with built-in HTTP caching (ETag/If-Modified-Since) score highly for Railway deployments because they prevent redundant API calls across ephemeral container restarts.

## 2026-04-09
- Field mapping systems using [from, to, options] tuple arrays provide reusable patterns for normalizing data from multiple external APIs into consistent internal schemas.

## 2026-04-09
- Go-based AI gateways with comprehensive architectural patterns score highly even when requiring language translation, because the provider abstraction and failover patterns are more valuable than direct code reuse.

## 2026-04-09
- Production-ready LLM gateways with cookbook examples for specific failover scenarios provide immediately actionable reliability improvements for multi-service content generation architectures.

## 2026-04-09
- Component registry platforms often contain more valuable UI patterns in their showcase components than in their registry infrastructure, but unknown license status makes pattern extraction legally risky.

## 2026-04-09
- Monorepos with 156+ dependencies and mixed runtimes (Bun + Node) score poorly for ease of adoption even when individual components demonstrate excellent React patterns.

## 2026-04-09
- Python version constraints that exclude newer versions (like <3.12) are immediate blockers for adoption, regardless of functional relevance.

## 2026-04-09
- ML-heavy Python projects with 80+ dependencies and native modules are generally incompatible with Node.js web service architectures, requiring pattern extraction rather than direct integration.

## 2026-04-09
- Speech-to-speech pipelines from major ML organizations provide valuable architectural patterns (handler abstractions, cancellation systems, streaming contexts) that can be extracted even when the full dependency stack is too heavy for adoption.

## 2026-04-09
- Unknown license status becomes a critical blocking factor for otherwise excellent codebases, especially from commercial organizations like Hugging Face where license complexity is common.

## 2026-04-09
- PostgreSQL-based job queues are particularly valuable for multi-service architectures because they eliminate the need for separate message broker infrastructure while providing ACID guarantees.

## 2026-04-09
- Job queue libraries that include production-ready monitoring dashboards using the same UI stack (React + Tailwind + shadcn/ui) score higher because they provide both infrastructure and reusable frontend patterns.
